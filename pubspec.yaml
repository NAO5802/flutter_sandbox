name: flutter_sandbox
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  webview_flutter: ^4.2.2
  http: ^1.1.0
  flutter_riverpod: ^2.3.10
  json_annotation: ^4.8.1
  shared_preferences: ^2.2.1
  path_provider: ^2.1.1
  sqflite: ^2.3.0
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.24.1
  patrol: ^2.1.0
  mockito: ^5.4.2
  build_runner: ^2.4.6
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true

patrol:
  app_name: flutter_sandbox
  android:
    package_name: com.mysandbox.flutter_sandbox
  ios:
    package_name: com.mysandbox.flutter_sandbox